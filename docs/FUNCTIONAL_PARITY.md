# Functional parity control document

This is the binding migration ledger. `VERIFIED` means API, React workflow,
authorization, data reconciliation and automated tests have all passed. A
domain row represents the feature family and is expanded into child rows as the
second-pass scanner discovers independently testable behavior.

The exact legacy REST child-route ledger is generated at
[`LEGACY_API_INVENTORY.md`](LEGACY_API_INVENTORY.md); the machine-readable
schema/form/report/service/command ledger is `legacy-surface.json`. A domain
cannot become `VERIFIED` while any corresponding child route remains open.

| ID | Module / feature | Legacy source and behavior | Data / rules / roles | New API / service / model | React surface | Tests | Status / verification / notes |
|---|---|---|---|---|---|---|---|
| IAM-001 | Authentication, sessions, users, MFA, recovery | `interface/login`, `src/Auth`, `login_mfa_registrations`, `api_token`, `api_refresh_token`, OAuth2 and portal auth/recovery | `users*`, `patient_access_onsite`, `api_*`, separate encrypted staff/portal MFA and reset tables; account state and ACL | `/api/v1/auth/*` and `/api/v1/portal/auth|mfa/*`; persisted staff/portal sessions, rotating refresh credentials, revocation, one-time reset tokens and encrypted TOTP registrations/challenges | Staff and portal login/MFA challenge, automatic renewal, logout, password recovery and self-service security workspaces | access revocation, refresh rotation/replay, non-enumeration/cooldown, one-time reset, encrypted TOTP, time-step/recovery replay, bounded challenge and portal/staff isolation | IN PROGRESS — both identity types have Argon2 login, short access tokens, server-side sessions, rotation/revocation, one-time recovery and encrypted TOTP MFA with recovery codes; legacy hash upgrade, password policy/history, WebAuthn/U2F, SSO, admin MFA reset and device/session administration remain |
| ACL-001 | Roles, groups, permissions and record access | `gacl/`, ACL checks throughout routes/pages | `gacl_*`, facility and patient-compartment constraints | explicit permission dependencies plus opt-in facility/warehouse scopes | permission-aware navigation/admin | deny/allow and scoped-resource matrix | IN PROGRESS — explicit route permissions and scheduling/inventory facility scopes exist; full phpGACL import, patient compartments and permission-aware navigation remain |
| PAT-001 | Demographics, contacts, search, merge and chart | patient services and `interface/patient_file` | `patient_data`, person/contact/address/telecom, related persons; duplicate and consent rules | `/api/v1/patients*`; patient repository | Patient search/create/chart | API, import and component tests | IN PROGRESS — typed core demographics, validated consent, lossless source payload and GET/POST/PATCH/PUT exist; contacts, history, duplicate merge, photos and full editor remain |
| SCH-001 | Calendar, appointments, holidays, reminders and flow board | `interface/main/calendar`, tracker and appointment services | `openemr_postcalendar_*`, `patient_tracker*`; recurrence/status/resource rules | scheduling router with facility scope, filters, resource conflicts, lifecycle, immutable flow events and lossless import | Facility-aware agenda list/create/actions and live patient-flow board | API, conflict, transition, scoped ACL, import helper and component tests | IN PROGRESS — appointments and patient flow are migrated; recurrence expansion, multi-provider events, categories, holidays, wait list and reminder delivery remain |
| ENC-001 | Encounters, lifecycle, forms, notes and signatures | `interface/forms`, `forms`, encounter services | `form_encounter`, `forms`, 35 form families; locks/signatures/amendments | encounter/form services | Encounter workspace and specialized editors | contract and workflows | IN PROGRESS — ten form families have source-verified contracts and React editors; linked clinical-note documents and procedure results now have reconciled import, patient-scoped audited workflows, lock enforcement and signature-integrity coverage; remaining families remain |
| CLI-001 | Problems, allergies, medications, surgery, vitals, immunization, care plans | lists and clinical/FHIR services | `lists`, `form_vitals`, `immunizations`, clinical plan/team/preference tables; coding/lifecycle rules | clinical domain APIs | Longitudinal chart modules | rule and FHIR parity | IN PROGRESS — relational plans preserve append-only goal/intervention status, progress and measured outcomes; polymorphic teams and typed preferences preserve codes, reasons, dates and history with audited React workflows, lossless import, and FHIR CarePlan/Goal/CareTeam read/search mappings; production reconciliation remains |
| ORD-001 | Prescriptions, labs, procedures, imaging and results | prescription/procedure/diagnostic services and forms | prescriptions, procedure order/result/report tables; signing/status/specimen rules | order/result services | Ordering and result review | lifecycle/integration | IN PROGRESS — simple Rx/lab records only |
| DOC-001 | Documents, templates, upload/download and legal signing | document services and `interface/patient_file/encounter` | `documents`, categories, legal master/detail; safe storage/version/signature rules | document metadata/blob service | Document center/viewer/signing | malware/path/access tests | IN PROGRESS — inline blob upload lacks legacy categories/versioning |
| FIN-001 | Insurance, eligibility, fee sheets, claims, X12, payments and statements | billing pages/services/reports and portal ledger/payment gateway | insurance, billing, claims, ar*, payments; exact monetary/state rules | core financial APIs plus explicitly released portal statements and idempotent token-only payment intents | Staff claim/release controls and portal balance/history | claim/payment tests plus portal isolation, idempotency, no-token-persistence and disabled-provider tests | IN PROGRESS — elementary coverage/claims, staff payments, released patient statements and processor-gated payment intents exist; allocations, adjustments, ERA/835, X12/837, full ledger and live processor adapters remain |
| INV-001 | Drugs, inventory, dispensing and sales | `interface/drugs`, `DrugService`, `DrugSalesService`, inventory reports | `drugs`, `drug_inventory`, `drug_sales`, `product_warehouse`; lot/warehouse/FEFO/no-negative-stock rules and granular inventory ACL | `/api/v1/inventory/*`; products, lots, immutable transactions and enforced warehouse scopes | Product catalog, scoped stock totals, lot and expiry workspace | API transaction/FEFO/rollback/scoped ACL and component tests | IN PROGRESS — core catalog, lots, stock movements, multi-lot dispensing, destruction, warehouse restrictions and lossless import migrated; vendors, reorder notifications, price levels and billing posting remain |
| COM-001 | Portal, messages, tasks, email, Direct and notifications | `portal/`, `patient_access_onsite`, portal ledger/gateways, `onsite_mail`, `onsite_messages`, `pnotes`, `form_taskman`, `email_queue`, message/email/notification services | separate portal identities, recovery/MFA and sessions, patient-bound threads, explicit clinical-record/statement release state, idempotent payment intents, identity audit, task lifecycle, consent and PHI-safe recipient policy | `/api/v1/portal/*`, staff release/revoke routes, `/api/v1/messages*`, `/api/v1/tasks*`, durable communication outbox; legacy credentials are discarded and reset is mandatory | Portal recovery/login/MFA/security, owned appointments, released results/documents/signed forms/statements; staff release and secure-message workspaces | token separation, session revocation, recovery non-enumeration, encrypted MFA/replay, patient isolation including guessed UUIDs, release gates, payment idempotency/no-token-storage/processor confirmation, identity audit, consent/outbox and task flows | IN PROGRESS — controlled records/statements, payment-intent boundary, secure messaging, revocable sessions, recovery and TOTP MFA, account provisioning, tasks and legacy import are implemented; live payment adapter, delivery worker, SMS, Direct, proxy access and notification preferences remain |
| REP-001 | Clinical, operational, financial, audit and quality reports | 48 surfaces in `interface/reports` and report/CQM services; see `REPORT_PARITY.md` | cross-domain read models; per-report ACL, facility/warehouse scope, filters and totals | `/api/v1/reports*`; catalog plus immutable report-run snapshots, SHA-256 and authenticated CSV | Complete catalog with pending status, filters, dynamic table, totals and export | exact catalog count, golden columns/rows, checksum, CSV, validation and deny tests | IN PROGRESS — 48/48 cataloged and 20 migrated, including IP-based login protection and reporting; 28 individually listed reports remain pending |
| INT-001 | Standard API, FHIR, SMART/OAuth, C-CDA/CCR/EHI, webhooks | three REST maps, `oauth2`, `ccdaservice`, `ccr` | tokens/scopes, patient compartments, export jobs; standards conformance | versioned compatibility APIs | API client administration | conformance/contract | IN PROGRESS — authenticated and audited FHIR clinical, diagnostic, questionnaire, scheduling, identity/directory, coverage, document and care-coordination surface with patient/facility scoping and resolvable references; SMART scopes, `$docref`, remaining resources, formal conformance and bulk export remain |
| ADM-001 | Facilities, providers, codes, templates, preferences, globals and modules | administration UI, `FacilityService`, `UserService`, practitioner roles and module hooks | `facility`, provider `users`, `users_facility`, warehouse `list_options`, codes/globals/module tables; admin-only policy | `/api/v1/admin/*`; facilities, practitioners, warehouses and user access grants | Administration console for facility/provider/warehouse catalogs | API, policy, scope, import and component tests | IN PROGRESS — facility/provider/warehouse catalogs and assignments migrated; user administration, codes, templates, preferences, globals and modules remain |
| OPS-001 | Audit, background jobs, imports, upgrades, localization and maintenance | audit pages, commands, background services, translations | `audit_*`, `background_services`, lang tables; tamper evidence/idempotency | independent audit seals, integrity report, import services | Generic report viewer; dedicated operations screens remain | checksum mismatch/deletion/unsealed detection, backfill and report ACL tests | IN PROGRESS — staff and portal/identity events receive transactional SHA3-512 seals and an admin-only tamper report; legacy detail-level before/after import, external anchoring, jobs, localization and maintenance remain |

No row may remain `NOT STARTED` or `IN PROGRESS` at completion. External
credentials alone may yield `BLOCKED`; internal engineering work is not a
blocker. No feature is currently intentionally removed.
