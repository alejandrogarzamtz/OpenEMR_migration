// @vitest-environment jsdom
import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it,vi} from "vitest";import {ReferralWorkspace} from "./ReferralWorkspace";
describe("ReferralWorkspace",()=>{it("creates and closes a referral",async()=>{let items:any[]=[];const api=vi.fn(async(path:string,init?:RequestInit)=>{if(init?.method==="POST"&&path.endsWith("/referrals")){items=[{uuid:"r1",...JSON.parse(String(init.body)),status:"requested"}];return items[0]}if(init?.method==="POST"){items[0]={...items[0],status:"completed",reply:"Completed"};return items[0]}return items}) as any;vi.spyOn(window,"prompt").mockReturnValue("Completed");render(<ReferralWorkspace api={api} patientUuid="p1"/>);fireEvent.click(await screen.findByText("New referral"));fireEvent.change(screen.getByPlaceholderText("Recipient"),{target:{value:"Specialist"}});fireEvent.change(screen.getByPlaceholderText("Reason"),{target:{value:"Review"}});fireEvent.change(document.querySelector('input[type="datetime-local"]')!,{target:{value:"2027-04-05T10:00"}});fireEvent.click(screen.getByText("Create referral"));await screen.findByText("Specialist");fireEvent.click(screen.getByText("Record reply"));expect(await screen.findByText("Completed")).toBeTruthy()})});
