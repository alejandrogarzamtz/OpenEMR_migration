// @vitest-environment jsdom
import {fireEvent,render,screen} from "@testing-library/react";import {describe,expect,it,vi} from "vitest";import {PatientEducationWorkspace} from "./PatientEducationWorkspace";
describe("PatientEducationWorkspace",()=>{it("loads resources and opens the server-generated search URL",async()=>{const api=vi.fn(async(path:string)=>path.includes("/search?")?{url:"https://example.org/search?q=heart%20health"}:[{uuid:"r1",name:"Medical Library",url_template:"https://example.org/search?q=[%]",sequence:1,active:true}]) as any;const open=vi.spyOn(window,"open").mockImplementation(()=>null);render(<PatientEducationWorkspace api={api}/>);fireEvent.change(await screen.findByLabelText("Education resource"),{target:{value:"r1"}});fireEvent.change(screen.getByLabelText("Education search"),{target:{value:"heart health"}});fireEvent.click(screen.getByText("Search resource"));await vi.waitFor(()=>expect(open).toHaveBeenCalledWith("https://example.org/search?q=heart%20health","_blank","noopener,noreferrer"));})});
